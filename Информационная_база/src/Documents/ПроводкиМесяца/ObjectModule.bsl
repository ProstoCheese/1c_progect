

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	Проводка = Движения.РегистрБухгалтерии.Добавить();
	Проводка.Период = КонецМесяца(ЭтотОбъект.Дата);
	Проводка.СчетДт = ПланыСчетов.БухУчет.Прибыль;
	Проводка.Сумма = ЭтотОбъект.Прибыль;
	
	 Движения.РегистрБухгалтерии.Записывать = Истина;
КонецПроцедуры
